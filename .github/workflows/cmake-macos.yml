name: 'Build on macOS'

on: 
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Build using CMake
      working-directory: ./build
      run: |
        cmake ..
        cmake --build .
